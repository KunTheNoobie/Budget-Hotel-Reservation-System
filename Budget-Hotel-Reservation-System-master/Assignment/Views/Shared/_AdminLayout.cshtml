@* 
    Admin Layout File
    Separate layout for Admin, Manager, and Staff roles
    Does NOT include customer-facing navigation items
*@
@using System.Security.Claims
@{
    var role = User.Claims.FirstOrDefault(c => c.Type == ClaimTypes.Role)?.Value ?? "Unknown";
}
@using Assignment.Helpers
@using Assignment.Models
@inject Assignment.Models.Data.HotelDbContext DbContext
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Admin Panel | BudgetHotel</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/Assignment.styles.css" asp-append-version="true" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Outfit:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --font-sans: 'Inter', sans-serif;
            --font-heading: 'Outfit', sans-serif;
        }
        body {
            font-family: var(--font-sans);
            background-color: #f8f9fa;
        }
        h1, h2, h3, h4, h5, h6, .navbar-brand {
            font-family: var(--font-heading);
        }
        .admin-navbar {
            background: linear-gradient(135deg, #6366f1 0%, #4f46e5 50%, #7c3aed 100%) !important;
            box-shadow: 0 4px 20px rgba(99, 102, 241, 0.25);
            padding: 1rem 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .admin-navbar .navbar-brand {
            font-size: 1.5rem;
            font-weight: 900;
            color: white !important;
            display: flex;
            align-items: center;
            letter-spacing: -0.5px;
        }
        
        .admin-navbar .navbar-brand i {
            font-size: 1.75rem;
            margin-right: 0.5rem;
        }
        
        .admin-navbar .nav-link {
            color: rgba(255, 255, 255, 0.95) !important;
            font-weight: 600;
            padding: 0.6rem 1.2rem !important;
            border-radius: 12px;
            transition: all 0.3s ease;
        }
        
        .admin-navbar .nav-link:hover {
            background-color: rgba(255, 255, 255, 0.2);
            color: white !important;
            transform: translateY(-1px);
        }
        
        .admin-navbar .dropdown-menu {
            border-radius: 16px;
            box-shadow: 0 12px 32px rgba(99, 102, 241, 0.15);
            border: 1px solid var(--border);
            margin-top: 0.5rem;
            padding: 0.5rem;
        }
        
        .admin-navbar .dropdown-item {
            border-radius: 12px;
            margin: 0.25rem 0.5rem;
            padding: 0.75rem 1.25rem;
            transition: all 0.3s ease;
            font-weight: 500;
        }
        
        .admin-navbar .dropdown-item:hover {
            background: linear-gradient(135deg, rgba(99, 102, 241, 0.1) 0%, rgba(16, 185, 129, 0.1) 100%);
            color: var(--primary) !important;
            transform: translateX(4px);
        }
        
        .admin-navbar .dropdown-item i {
            width: 20px;
            text-align: center;
            color: var(--primary);
        }
        
        body {
            background: linear-gradient(180deg, #ffffff 0%, #f8fafc 100%);
            min-height: 100vh;
        }
        
        main {
            background: transparent;
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark admin-navbar">
            <div class="container-fluid">
                <a class="navbar-brand" asp-controller="Admin" asp-action="Index">
                    <i class="bi bi-shield-check me-2"></i>BudgetStay @role
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbarContent" aria-controls="adminNavbarContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse" id="adminNavbarContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        @if (AuthenticationHelper.IsAuthenticated(Context))
                        {
                            var userRole = AuthenticationHelper.GetUserRole(Context);
                            
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                    <i class="bi bi-speedometer2 me-1"></i>@role
                                </a>
                                <ul class="dropdown-menu">
                                    <li><a class="dropdown-item" asp-controller="Admin" asp-action="Index"><i class="bi bi-house me-2"></i>Dashboard</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    
                                    @if (userRole == UserRole.Admin)
                                    {
                                        <li><a class="dropdown-item" asp-controller="Admin" asp-action="Users"><i class="bi bi-people me-2"></i>Users</a></li>
                                        <li><a class="dropdown-item" asp-controller="Admin" asp-action="Hotels"><i class="bi bi-building me-2"></i>Hotels</a></li>
                                    }
                                    else
                                    {
                                        <li><a class="dropdown-item" asp-controller="Admin" asp-action="Users"><i class="bi bi-people me-2"></i>Users</a></li>
                                    }
                                    
                                    @if (userRole == UserRole.Admin || userRole == UserRole.Manager)
                                    {
                                        <li><a class="dropdown-item" asp-controller="Admin" asp-action="RoomTypes"><i class="bi bi-door-open me-2"></i>Room Types</a></li>
                                        <li><a class="dropdown-item" asp-controller="Admin" asp-action="Rooms"><i class="bi bi-door-closed me-2"></i>Rooms</a></li>
                                    }
                                    
                                    <li><a class="dropdown-item" asp-controller="Admin" asp-action="Bookings"><i class="bi bi-calendar-check me-2"></i>Bookings</a></li>
                                    
                                    @if (userRole == UserRole.Admin || userRole == UserRole.Manager)
                                    {
                                        <li><a class="dropdown-item" asp-controller="Admin" asp-action="Reviews"><i class="bi bi-star me-2"></i>Reviews</a></li>
                                        <li><a class="dropdown-item" asp-controller="Admin" asp-action="Amenities"><i class="bi bi-list-check me-2"></i>Amenities</a></li>
                                        <li><a class="dropdown-item" asp-controller="Admin" asp-action="Packages"><i class="bi bi-box-seam me-2"></i>Packages</a></li>
                                        <li><a class="dropdown-item" asp-controller="Admin" asp-action="Promotions"><i class="bi bi-tag me-2"></i>Promotions</a></li>
                                    }
                                    
                                    @if (userRole == UserRole.Admin)
                                    {
                                        <li><a class="dropdown-item" asp-controller="Admin" asp-action="ContactMessages"><i class="bi bi-envelope me-2"></i>Contact Messages</a></li>
                                    }
                                </ul>
                            </li>
                        }
                    </ul>
                    
                    <ul class="navbar-nav ms-auto">
                        @if (AuthenticationHelper.IsAuthenticated(Context))
                        {
                            var userId = AuthenticationHelper.GetUserId(Context);
                            var userName = AuthenticationHelper.GetUserName(Context);
                            var userEmail = AuthenticationHelper.GetUserEmail(Context);
                            var userRole = AuthenticationHelper.GetUserRole(Context);
                            string? profilePictureUrl = null;
                            if (userId.HasValue)
                            {
                                var user = DbContext.Users.Find(userId.Value);
                                profilePictureUrl = user?.ProfilePictureUrl;
                            }
                            
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">
                                    @if (!string.IsNullOrEmpty(profilePictureUrl))
                                    {
                                        <img src="@profilePictureUrl" alt="@userName" class="rounded-circle me-2" style="width: 32px; height: 32px; object-fit: cover;" onerror="this.style.display='none'; this.nextElementSibling.style.display='inline-flex';">
                                        <span class="d-none">@userName?.Substring(0, 1).ToUpper()</span>
                                    }
                                    else
                                    {
                                        <span class="user-avatar me-2">@(userName?.Substring(0, 1).ToUpper() ?? userEmail?.Substring(0, 1).ToUpper())</span>
                                    }
                                    <span class="d-none d-md-inline">@(userName ?? userEmail)</span>
                                    <small class="ms-2 text-light opacity-75">(@userRole)</small>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li class="px-3 py-2">
                                        <div class="d-flex align-items-center">
                                            @if (!string.IsNullOrEmpty(profilePictureUrl))
                                            {
                                                <img src="@profilePictureUrl" alt="@userName" class="rounded-circle me-2" style="width: 40px; height: 40px; object-fit: cover;" onerror="this.style.display='none'; this.nextElementSibling.style.display='inline-flex';">
                                                <span class="d-none user-avatar-large">@(userName?.Substring(0, 1).ToUpper() ?? userEmail?.Substring(0, 1).ToUpper())</span>
                                            }
                                            else
                                            {
                                                <span class="user-avatar-large me-2">@(userName?.Substring(0, 1).ToUpper() ?? userEmail?.Substring(0, 1).ToUpper())</span>
                                            }
                                            <div>
                                                <div class="fw-bold">@userName</div>
                                                <small class="text-muted">@userEmail</small>
                                                <br><small class="badge bg-primary">@userRole</small>
                                            </div>
                                        </div>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" asp-controller="Customer" asp-action="Profile"><i class="bi bi-person-circle me-2"></i>My Profile</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" asp-controller="Security" asp-action="Logout"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
                                </ul>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    
    <!-- Loading Indicator -->
    <div id="loadingIndicator" class="position-fixed top-50 start-50 translate-middle" style="display: none; z-index: 9999; pointer-events: none;">
        <div class="bg-white rounded shadow-lg p-4 d-flex flex-column align-items-center">
            <div class="spinner-border text-primary mb-2" role="status" style="width: 3rem; height: 3rem;">
                <span class="visually-hidden">Loading...</span>
            </div>
            <small class="text-muted">Loading...</small>
        </div>
    </div>

    <!-- Toast Container -->
    <div id="toastContainer" class="toast-container position-fixed top-0 end-0 p-3" style="z-index: 9998;"></div>

    <main role="main" class="container-fluid py-4">
        @RenderBody()
    </main>

    <footer class="mt-auto py-4" style="background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%); border-top: 2px solid var(--border);">
        <div class="container-fluid">
            <div class="text-center text-muted small">
                <i class="bi bi-shield-check me-2" style="color: var(--primary);"></i>
                &copy; 2025 BudgetStay Admin Panel. All rights reserved.
            </div>
        </div>
    </footer>
    
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/dist/jquery.validate.unobtrusive.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script>
        // Initialize Toast Notifications from Server-Side TempData
        $(document).ready(function() {
            @if (TempData["Success"] != null)
            {
                <text>showToast('@Html.Raw(TempData["Success"])', 'success');</text>
            }
            @if (TempData["Error"] != null)
            {
                <text>showToast('@Html.Raw(TempData["Error"])', 'error');</text>
            }
            @if (TempData["Info"] != null)
            {
                <text>showToast('@Html.Raw(TempData["Info"])', 'info');</text>
            }
            @if (TempData["Warning"] != null)
            {
                <text>showToast('@Html.Raw(TempData["Warning"])', 'warning');</text>
            }
            @if (TempData["NewsletterError"] != null)
            {
                <text>showToast('@Html.Raw(TempData["NewsletterError"])', 'error');</text>
            }
            @if (TempData["NewsletterSuccess"] != null)
            {
                <text>showToast('@Html.Raw(TempData["NewsletterSuccess"])', 'success');</text>
            }
        });
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
